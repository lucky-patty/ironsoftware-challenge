/// <summary>
/// Just in case we can test several Phone to use / testing. We can add more bahavior but I think this is enough for testing the challenge
/// </summary>
public interface PhonePad {
    string ConvertMessage(string input);
}